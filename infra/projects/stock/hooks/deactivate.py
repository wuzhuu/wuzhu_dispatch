#!/usr/bin/env python3
from common import *
node=os.sys.argv[1] if len(os.sys.argv)>1 else "unspecified"
emit(True,node=node,action="descriptor_only_no_production_change",note="legacy scheduler remains paused until replacement is proven")
